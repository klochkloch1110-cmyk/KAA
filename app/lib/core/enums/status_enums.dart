enum OrderStatus { draft, assigned, inProgress, completed, cancelled, archived }

enum TripStatus { submitted, verified, needsReview, rejected }

enum OcrStatus { pending, matched, mismatch, failed, notRequired }

enum ShiftStatus { open, submitted, approved, needsReview }

enum VehicleStatus { active, service, repair, inactive }

